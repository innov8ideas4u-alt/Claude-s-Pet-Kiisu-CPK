"""Phase 3 test fixtures.

Currently empty — the Phase 3 reader-isolation tests use plain helper
functions (in ``_helpers.py``) rather than fixtures. This file exists so
pytest treats ``tests/phase3/`` as a recognised collection root and so
future fixture additions land in the conventional spot.
"""
